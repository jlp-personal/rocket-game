name: Build TypeScript

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
    
    - name: Install dependencies
      run: npm install
    
    - name: Build TypeScript
      run: npm run build
    
    - name: Commit compiled files
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add -f *.js
        git diff --quiet && git diff --staged --quiet || git commit -m "Auto-compile TypeScript to JavaScript"
    
    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: ${{ github.ref }}
```

### **3. Complete File Structure**

Here's what you'll have:
```
mission-control-game/
├── .github/
│   └── workflows/
│       └── build.yml        # Auto-compiles TypeScript
├── src/
│   ├── rocket.ts           # Your TypeScript Rocket classes
│   ├── game.ts             # Game logic in TypeScript
│   └── types.ts            # Shared interfaces
├── index.html              # Your game page
├── style.css               # Game styling
├── package.json            # Dependencies
├── tsconfig.json           # TypeScript config
├── README.md               # Instructions
├── game.js                 # AUTO-GENERATED by GitHub Action
└── rocket.js               # AUTO-GENERATED by GitHub Action
